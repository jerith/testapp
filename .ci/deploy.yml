platform: linux

image_resource:
  type: docker-image
  source:
    repository: python
    tag: 3-slim

inputs:
  - name: image-params

outputs:
  - name: deploy-params

run:
  path: ./testapp/.ci/deploy.sh
